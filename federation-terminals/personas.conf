# Federation terminal registry — consumed by launch-federation.sh
#
# Format (pipe-delimited, whitespace around | is ignored):
#     persona | workdir | session_id
#
#   persona     : label for the tab/window title (and used by AUTO discovery)
#   workdir     : directory the session lives in (the cwd claude was started in)
#   session_id  : a specific UUID to resume, OR the literal word  AUTO
#                 AUTO = at launch, find the newest *.jsonl in that workdir's
#                 project history that contains this persona's handle and
#                 resume it. Use AUTO if you start fresh sessions outside this
#                 script and don't want to hand-edit UUIDs.
#
# Lines starting with # are skipped — comment out a persona to not launch it
# (e.g. the one whose terminal you're already sitting in).
#
# Pinned UUIDs below were auto-discovered 2026-06-03. `claude --resume <id>`
# continues the same file in place, so these stay valid across relaunches.
# Run `./launch-federation.sh refresh` to re-derive them after starting any
# brand-new persona session.

# persona            | workdir            | session_id
qbp-architecture     | ~/Documents        | b332d4b7-2001-4f88-8cc1-a3a055a960c9
qbp-implementor      | ~/Documents        | 29656904-d93a-49d1-917e-a2a0441a0b29
cth-implementor      | ~/Documents        | c9eadd12-875a-4f4b-bf43-3401fb16e34e
bma-implementor      | ~/Documents        | b84d180a-3544-4a1e-a0af-f0c27db7724f
wyrd-implementor     | ~/Documents        | 61e677d9-c081-4e3b-9bf6-e5a30f2b0cea
herschel             | ~/Documents        | 9f7c150d-536b-4731-9ccd-f797d3f26554
cu-implementor       | ~/Documents        | 1734047c-670c-47c5-bece-3869fa482c90
contextus-impl       | ~/Documents        | 7babf319-4f74-4173-843c-37e6524063b8
qbp-oppenheimer      | ~/Documents/QBP    | cc9bae42-b88b-4399-8c1c-777c775ce9bd
