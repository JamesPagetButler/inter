# CV-Suite Baseline — main@cffb5b1 — 2026-06-14

> First runnable baseline of the Crawl-Completion Validation Suite (#86), post Wave-1/2/3 merge. Generated by qbp-architecture (worktree off origin/main). This is the starting state of the Sprint-3 close-loop.

## Run conditions
- `go test -p 2 -count=1 ./...` → **all 27 packages GREEN, 0 failures**
- `go test -p 2 -race -count=1 ./internal/bma/...` → **all 27 packages GREEN, 0 races** (CV-11.5 ✓)

## Headline
- **55 CV items** total (CV-1.1 … CV-13.4).
- **[UT] tier (runnable now): 21 GREEN · 0 RED · 5 no-test-yet (gaps).** The build waves delivered every runnable item that exists — nothing RED.
- **34 items gated** on [BOOT]/[LIVE] (→ #10 Step-9 instantiation), [72H] (→ #250 UPS + 72h run), or [MANUAL].

## The 5 [UT] gaps (triage required — most are NOT Crawl gaps)
| CV-id | Gap | Likely disposition |
|---|---|---|
| CV-1.4 | "type inference at write" — feature doesn't exist; `hg/infer.go` is duplicate-detection (KNN), not type inference; `WriteNode` takes caller-supplied type | **Spec-drift** — correct the CV (explicit typing is the design), don't implement |
| CV-7.3 | persona quaternion norm-drift + hallucination flag | `affect/` is a Walk-phase stub → **defer (written) + Walk issue**, or minimal Crawl impl |
| CV-10.5 | immune seam-watcher goroutine over CTH metrics | `immune/` is a Walk-phase interface → **defer (written) + Walk issue** |
| CV-11.3 | CTH integrity metrics η/μ/I/Δ/Re_e each sleep cycle | not implemented → **tier check**: Crawl or Walk? |
| CV-11.4 | D2′ proof-wiring CI gate (fails CI if artifact diverges from claimed proof) | **arguably a real Crawl gate** — it's the proven≠wired discipline we've lived all sprint; recommend implement |

## Spec-drift flags (the #267 accuracy class — found in the CV doc itself)
1. **CV-12.4** "14-phase boot" → actual **18** named phases (run.go). Correct the count.
2. **CV-1.4** "type inference at write" → code requires explicit type (see gap above).
3. **CV-3.1** "`SE_SLEEP_COMPLETE`" → code emits **`SE_SLEEP_DONE`**. ⚠ A CIV probe checking the spec'd name would **falsely fail** — fix before the close-loop runs.
4. **CV-10.4** "six instincts" but enumerates only **5** (Possum, Thermal, Sleep-Deferral, Crash-Recovery, Disk-Pressure). Confirm the 6th or correct the count.

## Gated-tier clusters (the real remaining work)
- **Cluster 1 — Step-9 instantiation (#10):** ~20 [LIVE]/[BOOT] items — CV-4.2/4.3/4.4, 5.1–5.4, 6.3, 7.1–7.2, 8.1–8.5, 9.1–9.4, 10.1/10.3, 12.3/12.4, 13.1–13.4.
- **Cluster 2 — UPS (#250) + 72h:** ~9 [72H] items — CV-1.5, 3.1–3.3, 4.1, 10.4, 10.6, 12.1, and **CV-12.2** (UPS hardware — not yet purchased; the hardest blocker).

## Notable architecture facts
- `hg/infer.go` is misleadingly named (duplicate-detection, not type inference).
- `affect/`, `cognitive/`, `curiosity/`, `immune/` are Walk-phase interfaces/stubs (no tests) — 4 CV items depend on them.
- Risk Ledger (#257) wired into sleep cycle, [UT] GREEN, but inits inside sleep-config not as a named boot phase.
- `testpodcycle` (#112 self-improvement loop) fully implemented + tested with a fake PodHelper; real path needs `bma-pod-helper` daemon.

## Close path
1. **Correct the 4 spec-drift flags** in the CV doc (esp. FLAG-3 event-name → else false CIV failures). Quick.
2. **Triage the 5 [UT] gaps** — implement (CV-11.4) vs written-deferral+Walk-issue (CV-7.3/10.5) vs spec-correct (CV-1.4) vs tier-check (CV-11.3).
3. **[UT] gate then closes GREEN** — the runnable half of Crawl-close is essentially met.
4. **Capstone (beekeeper):** #10 Step-9 instantiation unlocks Cluster 1; #250 UPS + 72h unlocks Cluster 2 → **notary D6 re-derives** → Crawl/Sprint-3 closes.

> **Reconciliation note (2026-06-14):** this baseline ran against a then-stale local CV doc missing **CV-7.4** (Pentagon swap #258) and **CV-9.5** (NATS #249). Both are present on canonical main and **GREEN [UT]** via the merged #264/#269 — so the true tally is **~23 [UT] GREEN / 0 RED of 57**, not 21/55. The CV doc status matrix is reconciled.
